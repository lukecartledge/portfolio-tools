name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    name: ci
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: 💅 Prettier
        run: npx prettier --check .

      - name: 🔍 ESLint
        run: npx eslint src/

      - name: 🔒 Type check
        run: npx tsc --noEmit

      - name: 🧪 Run tests
        run: npm test
